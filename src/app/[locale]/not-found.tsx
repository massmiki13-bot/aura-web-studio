import { NotFoundPage } from "@/components/pages/NotFoundPage";

/** Raised from inside the locale segment — see NotFoundPage for the split. */
export default function NotFound() {
  return <NotFoundPage />;
}
