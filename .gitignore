# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

node_modules

# Next.js build output
.next/
out/
next-env.d.ts

# Vercel
.vercel

# Env files (secrets — mai committare; configurarle su Vercel).
# .env.example is the documented template and is deliberately tracked.
.env
.env.*
!.env.example

# Lockfile Bun: il progetto usa npm (package-lock.json); un bun.lock
# committato ha priorità su Vercel e fa buildare con versioni diverse
bun.lock
bun.lockb

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript incremental build cache
*.tsbuildinfo
